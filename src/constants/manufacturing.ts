/**
 * Manufacturing constants shared across the harness pipeline and UI.
 *
 * Governance:
 *   - Never hardcode machine / brand names as raw strings in services or UI.
 *   - Always import from here to ensure consistent terminology.
 */

/** The primary wire-cutting and processing machine used in production. */
export const MACHINE_KOMAX = 'Komax';
